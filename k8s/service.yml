apiVersion: v1
kind: Service
metadata:
  name: hyuytytt
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "hyuytytt"
    port: 8080
  selector:
    app: hyuytytt
